# Implementation Tasks: Fix Wayland Clipboard Copying When Application Window is Hidden

- [x] 1. Add `wl-copy` fallback to `copy_to_clipboard()` in `TesseractTrigger.vala` <!-- id: 1 -->
- [x] 2. Build, install to `~/.local/bin` and `/usr/local/bin`, and verify clipboard copying from tray <!-- id: 2 -->
