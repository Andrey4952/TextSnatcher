# Implementation Tasks: Fix Tray Screenshot — No Window Popup

- [x] 1. Add `get_label_only()` to `CosmicTray.vala` and switch screenshot/clipboard/file call sites to use it <!-- id: 1 -->
- [x] 2. Add `get_label_only()` to `TrayIcon.vala` and switch screenshot/clipboard/file call sites to use it <!-- id: 2 -->
- [x] 3. Build, install, and verify: take screenshot from tray while window is hidden — window must stay hidden <!-- id: 3 -->
