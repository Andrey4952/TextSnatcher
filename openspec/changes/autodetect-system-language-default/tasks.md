# Implementation Tasks: Auto-detect System Language Default for OCR

- [x] 1. Update `load_config()` in `LanguageButton.vala` to auto-detect system locale fallback <!-- id: 1 -->
- [x] 2. Build, install, and verify application compilation <!-- id: 2 -->
